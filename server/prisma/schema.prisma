generator client {
  provider     = "prisma-client"
  output       = "../generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String @id @default(nanoid(32))
  firstName String
  lastName  String
  email     String @unique
  password  String
}
